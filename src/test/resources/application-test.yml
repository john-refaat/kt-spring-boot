spring:
  data:
    mongodb:
      database: testdb
      # DO NOT set uri here → lets Spring Boot use embedded Mongo
logging:
  level:
    org.mongodb.driver.cluster: ERROR
    org.mongodb.driver.connection: ERROR